# SCS Competitions Homepage

## Getting Started

- Clone the repository.
- Install Node.js (latest).
- `cd /path/to/repository`
- `npm install -g gulp`
- `npm install`
- `gulp` builds/minifies all files for you and starts a dev server
